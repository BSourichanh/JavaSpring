meta {
  name: 5. Vérifier Alice Valide (:8081)
  type: http
  seq: 5
}

get {
  url: {{userServiceUrl}}/users/{{aliceId}}/valid
  body: none
  auth: none
}

tests {
  test("Code HTTP 200 OK", function() {
    expect(res.getStatus()).to.equal(200);
  });
  
  test("Alice est bien reconnue valide (true)", function() {
    expect(res.getBody()).to.equal(true);
  });
}
